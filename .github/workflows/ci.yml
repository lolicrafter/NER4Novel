name: CI

on:
  push:
    branches: [ main, master, develop, feature/**, fix/** ]
  pull_request:
    branches: [ main, master, develop, feature/**, fix/** ]
  workflow_dispatch:

jobs:
  setup-pyhanlp-data:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Log environment info
      run: |
        echo "=== 环境信息 ==="
        echo "Runner OS: $(uname -a)"
        echo "Python version request: 3.8"
        echo "Working directory: $(pwd)"
        echo "Git branch: ${{ github.ref }}"
        echo "Git commit: ${{ github.sha }}"
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.8"  # 统一使用 Python 3.8
    
    - name: Verify Python installation
      run: |
        echo "=== Python 安装信息 ==="
        python --version
        pip --version
        echo "Python path: $(which python)"
        echo "Pip path: $(which pip)"
    
    - name: Install pyhanlp
      run: |
        echo "=== 开始安装 pyhanlp ==="
        python -m pip install --upgrade pip
        echo "✅ pip 升级完成"
        echo "📦 开始安装 pyhanlp..."
        pip install pyhanlp
        echo "✅ pyhanlp 安装完成"
        pip show pyhanlp || echo "⚠️ pyhanlp 包信息获取失败"
    
    - name: Get pyhanlp static directory
      id: pyhanlp-path
      run: |
        SITE_PACKAGES=$(python -c "import site; print(site.getsitepackages()[0])" 2>/dev/null || python -c "from distutils.sysconfig import get_python_lib; print(get_python_lib())")
        PYHANLP_STATIC="$SITE_PACKAGES/pyhanlp/static"
        echo "static_path=$PYHANLP_STATIC" >> $GITHUB_OUTPUT
        echo "PyHanLP static directory: $PYHANLP_STATIC"
    
    - name: Cache pyhanlp data file
      id: cache-pyhanlp-data
      uses: actions/cache@v4
      with:
        path: /tmp/pyhanlp-data-cache
        key: ${{ runner.os }}-pyhanlp-data-file-1.8.6-v2
        restore-keys: |
          ${{ runner.os }}-pyhanlp-data-file-
    
    - name: Log cache status
      run: |
        echo "=== 缓存状态 ==="
        CACHE_HIT="${{ steps.cache-pyhanlp-data.outputs.cache-hit }}"
        echo "Cache hit: $CACHE_HIT"
        if [ "$CACHE_HIT" == "true" ]; then
          echo "✅ 使用缓存的 HanLP 数据文件"
          if [ -f /tmp/pyhanlp-data-cache/data-for-1.8.6.zip ]; then
            FILE_SIZE=$(du -h /tmp/pyhanlp-data-cache/data-for-1.8.6.zip | cut -f1)
            echo "缓存文件大小: $FILE_SIZE"
          fi
        else
          echo "📥 缓存未命中，需要下载"
        fi
    
    - name: Download pyhanlp data file
      if: steps.cache-pyhanlp-data.outputs.cache-hit != 'true'
      run: |
        mkdir -p /tmp/pyhanlp-data-cache
        DATA_URL="https://file.hankcs.com/hanlp/data-for-1.8.6.zip"
        CACHE_FILE="/tmp/pyhanlp-data-cache/data-for-1.8.6.zip"
        
        echo "📥 Cache miss - downloading data file from $DATA_URL..."
        if command -v wget &> /dev/null; then
          wget --progress=bar:force:noscroll --timeout=300 --tries=3 -O "$CACHE_FILE" "$DATA_URL" || {
            echo "⚠️ wget failed, trying alternative URL..."
            DATA_URL="https://file.hankcs.com/hanlp/data-for-1.7.5.zip"
            wget --progress=bar:force:noscroll --timeout=300 --tries=3 -O "$CACHE_FILE" "$DATA_URL"
          }
        else
          curl -L --progress-bar --max-time 300 --retry 3 -o "$CACHE_FILE" "$DATA_URL" || {
            echo "⚠️ curl failed, trying alternative URL..."
            DATA_URL="https://file.hankcs.com/hanlp/data-for-1.7.5.zip"
            curl -L --progress-bar --max-time 300 --retry 3 -o "$CACHE_FILE" "$DATA_URL"
          }
        fi
        
        if [ -f "$CACHE_FILE" ]; then
          FILE_SIZE=$(du -h "$CACHE_FILE" | cut -f1)
          echo "✅ Download completed: $FILE_SIZE"
          echo "💾 Cache will be saved automatically at end of job"
        else
          echo "❌ Download failed"
          exit 1
        fi
    - name: Verify cache status
      if: always()
      run: |
        if [ "${{ steps.cache-pyhanlp-data.outputs.cache-hit }}" == "true" ]; then
          echo "✅ Cache hit - using cached data file"
          FILE_SIZE=$(du -h /tmp/pyhanlp-data-cache/data-for-1.8.6.zip 2>/dev/null | cut -f1 || echo "unknown")
          echo "📦 Cached file size: $FILE_SIZE"
        else
          echo "📥 Cache miss - downloaded new file"
          if [ -f /tmp/pyhanlp-data-cache/data-for-1.8.6.zip ]; then
            FILE_SIZE=$(du -h /tmp/pyhanlp-data-cache/data-for-1.8.6.zip | cut -f1)
            echo "💾 New file will be cached (size: $FILE_SIZE)"
          fi
        fi

  test:
    needs: [setup-pyhanlp-data, test-api-optimized]
    runs-on: ubuntu-latest
    # 顺序执行：test 等待 test-api-optimized 完成后再运行，确保 artifact 可用
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '8'
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.8"
    
    - name: Log environment info
      run: |
        echo "=== 环境信息 ==="
        echo "Runner OS: $(uname -a)"
        echo "Python version request: 3.8"
        echo "Working directory: $(pwd)"
        echo "Git branch: ${{ github.ref }}"
        echo "Git commit: ${{ github.sha }}"
        echo "Job: test"
    
    - name: Verify Java installation
      run: |
        echo "=== Java 环境信息 ==="
        echo "JAVA_HOME=$JAVA_HOME"
        if [ -n "$JAVA_HOME" ]; then
          echo "✅ JAVA_HOME 已设置"
        else
          echo "⚠️ JAVA_HOME 未设置"
        fi
        echo "Java 版本:"
        java -version 2>&1 || echo "❌ java 命令不可用"
        echo "Java 编译器版本:"
        javac -version 2>&1 || echo "❌ javac 命令不可用"
        echo "Java 路径: $(which java 2>/dev/null || echo '未找到')"
    
    - name: Cache pip packages
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install system fonts for Chinese characters
      run: |
        sudo apt-get update
        sudo apt-get install -y fonts-wqy-zenhei fonts-wqy-microhei || echo "Font installation failed, will use fallback"
    
    - name: Install dependencies
      run: |
        echo "=== 开始安装依赖 ==="
        python -m pip install --upgrade pip
        echo "✅ pip 升级完成: $(pip --version)"
        
        echo "📦 安装 jpype1==0.7.0..."
        pip install jpype1==0.7.0
        pip show jpype1 | grep Version || echo "⚠️ jpype1 版本检查失败"
        
        echo "📦 安装基础依赖..."
        pip install tqdm numpy networkx matplotlib scipy
        echo "✅ 基础依赖安装完成"
        
        echo "📦 安装数据处理依赖..."
        pip install requests>=2.28.0 pandas>=1.3.0 openpyxl>=3.0.0
        echo "✅ 数据处理依赖安装完成"
        
        echo "📦 安装 pyhanlp 相关依赖..."
        pip install hanlp-downloader
        echo "✅ hanlp-downloader 安装完成"
        
        echo "📦 安装 pyhanlp（不包含依赖）..."
        pip install --no-deps pyhanlp || pip install pyhanlp
        echo "✅ pyhanlp 安装完成"
        
        echo "📦 确保 jpype1==0.7.0 版本..."
        pip install --force-reinstall --no-deps jpype1==0.7.0
        echo "✅ jpype1 版本确认完成"
        # Clear matplotlib font cache to detect newly installed fonts
        # Use the correct API for newer matplotlib versions
        python -c "
        try:
            from matplotlib import font_manager
            font_manager._rebuild()
            print('Font cache rebuilt successfully')
        except AttributeError:
            try:
                # For newer matplotlib versions
                import matplotlib
                matplotlib.font_manager.fontManager.__class__._rebuild()
                print('Font cache rebuilt successfully (new API)')
            except Exception as e:
                print(f'Font cache rebuild failed: {e}')
        " || echo "Font cache rebuild failed"
        # Verify JPype can find Java after installation
        python -c "import jpype; print('JPype version:', jpype.__version__); print('JPype JVM path:', jpype.getDefaultJVMPath())" || echo "JPype installation check completed"
    
    - name: Get pyhanlp static directory
      id: pyhanlp-path
      run: |
        # Get site-packages directory without importing pyhanlp (to avoid JVM startup)
        SITE_PACKAGES=$(python -c "import site; print(site.getsitepackages()[0])" 2>/dev/null || python -c "from distutils.sysconfig import get_python_lib; print(get_python_lib())")
        PYHANLP_STATIC="$SITE_PACKAGES/pyhanlp/static"
        echo "static_path=$PYHANLP_STATIC" >> $GITHUB_OUTPUT
        echo "PyHanLP static directory: $PYHANLP_STATIC"
        # Verify the path exists (pyhanlp package should be installed)
        if [ -d "$SITE_PACKAGES/pyhanlp" ]; then
          echo "PyHanLP package found at: $SITE_PACKAGES/pyhanlp"
        else
          echo "Warning: PyHanLP package directory not found"
        fi
    
    - name: Cache pyhanlp data file
      id: cache-pyhanlp-data
      uses: actions/cache@v4
      with:
        path: /tmp/pyhanlp-data-cache
        key: ${{ runner.os }}-pyhanlp-data-file-1.8.6-v2
        restore-keys: |
          ${{ runner.os }}-pyhanlp-data-file-
    
    - name: Download pyhanlp data file if not cached
      if: steps.cache-pyhanlp-data.outputs.cache-hit != 'true'
      run: |
        mkdir -p /tmp/pyhanlp-data-cache
        DATA_URL="https://file.hankcs.com/hanlp/data-for-1.8.6.zip"
        CACHE_FILE="/tmp/pyhanlp-data-cache/data-for-1.8.6.zip"
        
        echo "📥 Cache miss - downloading data file from $DATA_URL..."
        if command -v wget &> /dev/null; then
          wget --progress=bar:force:noscroll --timeout=300 --tries=3 -O "$CACHE_FILE" "$DATA_URL" || {
            echo "⚠️ wget failed, trying alternative URL..."
            DATA_URL="https://file.hankcs.com/hanlp/data-for-1.7.5.zip"
            wget --progress=bar:force:noscroll --timeout=300 --tries=3 -O "$CACHE_FILE" "$DATA_URL"
          }
        else
          curl -L --progress-bar --max-time 300 --retry 3 -o "$CACHE_FILE" "$DATA_URL" || {
            echo "⚠️ curl failed, trying alternative URL..."
            DATA_URL="https://file.hankcs.com/hanlp/data-for-1.7.5.zip"
            curl -L --progress-bar --max-time 300 --retry 3 -o "$CACHE_FILE" "$DATA_URL"
          }
        fi
        
        if [ -f "$CACHE_FILE" ]; then
          FILE_SIZE=$(du -h "$CACHE_FILE" | cut -f1)
          echo "✅ Download completed: $FILE_SIZE"
          echo "💾 Cache will be saved automatically at end of job"
        else
          echo "❌ Download failed"
          exit 1
        fi
    
    - name: Verify cache status
      if: always()
      run: |
        if [ "${{ steps.cache-pyhanlp-data.outputs.cache-hit }}" == "true" ]; then
          echo "✅ Cache hit - using cached data file"
          FILE_SIZE=$(du -h /tmp/pyhanlp-data-cache/data-for-1.8.6.zip 2>/dev/null | cut -f1 || echo "unknown")
          echo "📦 Cached file size: $FILE_SIZE"
        else
          echo "📥 Cache miss in test job - will download"
          if [ -f /tmp/pyhanlp-data-cache/data-for-1.8.6.zip ]; then
            FILE_SIZE=$(du -h /tmp/pyhanlp-data-cache/data-for-1.8.6.zip | cut -f1)
            echo "💾 File downloaded (size: $FILE_SIZE), will be cached for next run"
          fi
        fi
    
    - name: Install pyhanlp data file
      if: steps.pyhanlp-path.outputs.static_path != ''
      run: |
        PYHANLP_STATIC="${{ steps.pyhanlp-path.outputs.static_path }}"
        mkdir -p "$PYHANLP_STATIC"
        
        if [ -f /tmp/pyhanlp-data-cache/data-for-1.8.6.zip ]; then
          echo "✅ Copying data file from cache to pyhanlp static directory"
          cp /tmp/pyhanlp-data-cache/data-for-1.8.6.zip "$PYHANLP_STATIC/data-for-1.8.6.zip"
          FILE_SIZE=$(du -h "$PYHANLP_STATIC/data-for-1.8.6.zip" | cut -f1)
          echo "✅ Data file installed at $PYHANLP_STATIC/data-for-1.8.6.zip (size: $FILE_SIZE)"
        else
          echo "⚠️ Warning: Cache file not found, pyhanlp may download automatically on first use"
        fi
    
    - name: Verify Python imports
      run: |
        echo "=== 验证 Python 导入 ==="
        echo "测试基础依赖..."
        python -c "import tqdm; print('✅ tqdm')" || echo "❌ tqdm 导入失败"
        python -c "import numpy; print('✅ numpy', numpy.__version__)" || echo "❌ numpy 导入失败"
        python -c "import networkx; print('✅ networkx', networkx.__version__)" || echo "❌ networkx 导入失败"
        python -c "import matplotlib; print('✅ matplotlib', matplotlib.__version__)" || echo "❌ matplotlib 导入失败"
        python -c "import pandas; print('✅ pandas')" || echo "❌ pandas 导入失败"
        python -c "import requests; print('✅ requests')" || echo "❌ requests 导入失败"
        echo "测试 HanLP..."
        python -c "import pyhanlp; print('✅ PyHanLP import successful')" || echo "⚠️ PyHanLP import failed (may need data file)"
    
    - name: Check code syntax
      run: |
        echo "=== 代码语法检查 ==="
        echo "检查 rel.py..."
        python -m py_compile rel.py && echo "✅ rel.py 语法正确" || echo "❌ rel.py 语法错误"
        echo "检查 rel_api_optimized.py..."
        python -m py_compile rel_api_optimized.py && echo "✅ rel_api_optimized.py 语法正确" || echo "❌ rel_api_optimized.py 语法错误"
        echo "检查 nlp_demos 目录..."
        find nlp_demos -name "*.py" -exec python -m py_compile {} \; && echo "✅ nlp_demos 语法检查完成" || echo "⚠️ nlp_demos 部分文件可能有语法错误"
      continue-on-error: true
    
    - name: Test with sample book (if exists)
      env:
        CI: true
        MPLBACKEND: Agg
      run: |
        mkdir -p output
        echo "Checking for test books..."
        ls -la book/ 2>/dev/null || echo "book/ directory not found"
        
        # Verify JPype before running test
        echo "Verifying JPype and Java setup..."
        python -c "
        import jpype
        import os
        import sys
        
        print(f'JPype version: {jpype.__version__}')
        print(f'JAVA_HOME: {os.environ.get(\"JAVA_HOME\", \"not set\")}')
        print(f'JPype JVM path: {jpype.getDefaultJVMPath()}')
        " || echo "JPype verification failed"
        
        # 动态查找 book 目录下的书籍文件
        echo "=== 查找测试书籍 ==="
        echo "搜索 book/ 目录..."
        if [ -d "book" ]; then
          echo "✅ book/ 目录存在"
          echo "book/ 目录内容:"
          ls -lah book/ || echo "无法列出 book/ 目录内容"
        else
          echo "⚠️ book/ 目录不存在"
        fi
        
        BOOK_FILE=$(find book -maxdepth 1 -name "*.txt" -type f | head -n 1)
        echo "找到的书籍文件: ${BOOK_FILE:-无}"
        
        if [ -n "$BOOK_FILE" ] && [ -f "$BOOK_FILE" ]; then
          # 提取文件名（不含路径和扩展名）
          BOOK_NAME=$(basename "$BOOK_FILE" .txt)
          BOOK_SIZE=$(du -h "$BOOK_FILE" | cut -f1)
          echo "=== 测试书籍信息 ==="
          echo "✅ 找到测试书籍: $BOOK_NAME"
          echo "📄 文件路径: $BOOK_FILE"
          echo "📊 文件大小: $BOOK_SIZE"
          echo "🚀 开始运行测试..."
          python rel.py --book "$BOOK_NAME" || {
            echo "⚠️ Test failed, but continuing..."
            echo "Test error details above"
          }
        else
          echo "No test book found in book/ directory, skipping execution test"
          # 创建一个占位文件，确保 output 目录存在
          echo "No test output - book file not available" > output/README.txt
        fi
        
        # 列出所有生成的文件
        echo "Generated files in output directory:"
        find output -type f 2>/dev/null || echo "No files found in output/"
        echo "Files in root directory:"
        find . -maxdepth 1 -name "*.png" -o -name "*.jpg" -o -name "*.pdf" 2>/dev/null || echo "No image files in root"
      continue-on-error: true
    
    - name: List generated files for debugging
      if: always()
      run: |
        echo "=== Files in output directory ==="
        ls -lah output/ 2>/dev/null || echo "output/ directory does not exist"
        echo "=== Files in root directory ==="
        ls -lah *.png *.jpg *.pdf 2>/dev/null || echo "No image files in root"
        echo "=== All files recursively ==="
        find . -name "*.png" -o -name "*.jpg" -o -name "*.pdf" 2>/dev/null | head -20 || echo "No image files found"
      continue-on-error: true
    
    - name: Download API optimized results artifact
      if: always()
      continue-on-error: true
      uses: actions/download-artifact@v4
      with:
        name: api-optimized-results
        path: api-output/
    
    - name: Log artifact download status
      if: always()
      run: |
        echo "=== Artifact 下载状态 ==="
        if [ -d "api-output" ] && [ "$(ls -A api-output 2>/dev/null)" ]; then
          echo "✅ 成功下载 api-optimized-results artifact"
          echo "目录内容:"
          ls -lah api-output/ || echo "无法列出目录内容"
        else
          echo "⚠️ api-optimized-results artifact 不存在或为空"
          echo "💡 可能的原因："
          echo "   - test-api-optimized job 失败或未上传 artifact"
          echo "   - API_KEY 未设置，test-api-optimized 仅进行了语法检查"
          echo "   - 测试书籍不存在，test-api-optimized 跳过了运行"
          echo ""
          echo "注意：test job 已等待 test-api-optimized 完成，如果 artifact 不存在，说明 test-api-optimized 可能未生成结果"
        fi
      continue-on-error: true
    
    - name: Upload test results as artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results-python-3.8
        path: |
          output/
        if-no-files-found: warn
        retention-days: 7
      continue-on-error: true

  test-api-optimized:
    needs: setup-pyhanlp-data
    runs-on: ubuntu-latest
    timeout-minutes: 30
    # 如果使用 Environment secrets，取消下面的注释并设置环境名称
    # environment: production  # 或 staging、development 等
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '8'
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.8"  # 使用 3.8 以兼容 JPype1==0.7.0
    
    - name: Log environment info
      run: |
        echo "=== 环境信息 ==="
        echo "Runner OS: $(uname -a)"
        echo "Python version request: 3.8"
        echo "Working directory: $(pwd)"
        echo "Git branch: ${{ github.ref }}"
        echo "Git commit: ${{ github.sha }}"
        echo "Job: test-api-optimized"
        echo "Timeout: 30 minutes"
    
    - name: Verify Java installation
      run: |
        echo "=== Java 环境信息 ==="
        echo "JAVA_HOME=$JAVA_HOME"
        if [ -n "$JAVA_HOME" ]; then
          echo "✅ JAVA_HOME 已设置"
        else
          echo "⚠️ JAVA_HOME 未设置"
        fi
        echo "Java 版本:"
        java -version 2>&1 || echo "❌ java 命令不可用"
        echo "Java 路径: $(which java 2>/dev/null || echo '未找到')"
    
    - name: Verify Python installation
      run: |
        echo "=== Python 安装信息 ==="
        python --version
        pip --version
        echo "Python path: $(which python)"
        echo "Pip path: $(which pip)"
    
    - name: Cache pip packages
      id: cache-pip-api
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Log pip cache status
      run: |
        echo "=== Pip 缓存状态 ==="
        CACHE_HIT="${{ steps.cache-pip-api.outputs.cache-hit }}"
        echo "Cache hit: $CACHE_HIT"
        if [ "$CACHE_HIT" == "true" ]; then
          echo "✅ 使用缓存的 pip 包"
        else
          echo "📥 缓存未命中，将重新安装依赖"
        fi
        echo "Requirements.txt hash: ${{ hashFiles('requirements.txt') }}"
    
    - name: Install system fonts for Chinese characters
      run: |
        sudo apt-get update
        sudo apt-get install -y fonts-wqy-zenhei fonts-wqy-microhei || echo "Font installation failed, will use fallback"
    
    - name: Install Python dependencies
      run: |
        echo "=== 开始安装依赖 ==="
        python -m pip install --upgrade pip
        echo "✅ pip 升级完成: $(pip --version)"
        
        echo "📦 安装基础依赖..."
        pip install tqdm numpy networkx matplotlib scipy
        echo "✅ 基础依赖安装完成"
        
        echo "📦 安装数据处理依赖..."
        pip install requests>=2.28.0 pandas>=1.3.0 openpyxl>=3.0.0
        echo "✅ 数据处理依赖安装完成"
        
        echo "📦 安装 HanLP 相关依赖..."
        echo "安装 jpype1==0.7.0..."
        pip install jpype1==0.7.0
        pip show jpype1 | grep Version || echo "⚠️ jpype1 版本检查失败"
        
        echo "安装 pyhanlp（不包含依赖）..."
        pip install --no-deps pyhanlp || pip install pyhanlp
        echo "✅ pyhanlp 安装完成"
        
        echo "确保 jpype1==0.7.0 版本..."
        pip install --force-reinstall --no-deps jpype1==0.7.0
        echo "✅ jpype1 版本确认完成"
        # Clear matplotlib font cache to detect newly installed fonts
        python -c "
        try:
            from matplotlib import font_manager
            font_manager._rebuild()
            print('Font cache rebuilt successfully')
        except AttributeError:
            try:
                import matplotlib
                matplotlib.font_manager.fontManager.__class__._rebuild()
                print('Font cache rebuilt successfully (new API)')
            except Exception as e:
                print(f'Font cache rebuild failed: {e}')
        " || echo "Font cache rebuild failed"
        # Verify JPype can find Java after installation
        python -c "import jpype; print('JPype version:', jpype.__version__); print('JPype JVM path:', jpype.getDefaultJVMPath())" || echo "JPype installation check completed"
    
    - name: Get pyhanlp static directory
      id: pyhanlp-path
      run: |
        SITE_PACKAGES=$(python -c "import site; print(site.getsitepackages()[0])" 2>/dev/null || python -c "from distutils.sysconfig import get_python_lib; print(get_python_lib())")
        PYHANLP_STATIC="$SITE_PACKAGES/pyhanlp/static"
        echo "static_path=$PYHANLP_STATIC" >> $GITHUB_OUTPUT
        echo "PyHanLP static directory: $PYHANLP_STATIC"
    
    - name: Cache pyhanlp data file
      id: cache-pyhanlp-data
      uses: actions/cache@v4
      with:
        path: /tmp/pyhanlp-data-cache
        key: ${{ runner.os }}-pyhanlp-data-file-1.8.6-v2
        restore-keys: |
          ${{ runner.os }}-pyhanlp-data-file-
    
    - name: Download pyhanlp data file if not cached
      if: steps.cache-pyhanlp-data.outputs.cache-hit != 'true'
      run: |
        mkdir -p /tmp/pyhanlp-data-cache
        DATA_URL="https://file.hankcs.com/hanlp/data-for-1.8.6.zip"
        CACHE_FILE="/tmp/pyhanlp-data-cache/data-for-1.8.6.zip"
        
        echo "📥 Cache miss - downloading data file from $DATA_URL..."
        if command -v wget &> /dev/null; then
          wget --progress=bar:force:noscroll --timeout=300 --tries=3 -O "$CACHE_FILE" "$DATA_URL" || {
            echo "⚠️ wget failed, trying alternative URL..."
            DATA_URL="https://file.hankcs.com/hanlp/data-for-1.7.5.zip"
            wget --progress=bar:force:noscroll --timeout=300 --tries=3 -O "$CACHE_FILE" "$DATA_URL"
          }
        else
          curl -L --progress-bar --max-time 300 --retry 3 -o "$CACHE_FILE" "$DATA_URL" || {
            echo "⚠️ curl failed, trying alternative URL..."
            DATA_URL="https://file.hankcs.com/hanlp/data-for-1.7.5.zip"
            curl -L --progress-bar --max-time 300 --retry 3 -o "$CACHE_FILE" "$DATA_URL"
          }
        fi
        
        if [ -f "$CACHE_FILE" ]; then
          FILE_SIZE=$(du -h "$CACHE_FILE" | cut -f1)
          echo "✅ Download completed: $FILE_SIZE"
        else
          echo "❌ Download failed"
          exit 1
        fi
    
    - name: Install pyhanlp data file
      if: steps.pyhanlp-path.outputs.static_path != ''
      run: |
        PYHANLP_STATIC="${{ steps.pyhanlp-path.outputs.static_path }}"
        mkdir -p "$PYHANLP_STATIC"
        
        if [ -f /tmp/pyhanlp-data-cache/data-for-1.8.6.zip ]; then
          echo "✅ Copying data file from cache to pyhanlp static directory"
          cp /tmp/pyhanlp-data-cache/data-for-1.8.6.zip "$PYHANLP_STATIC/data-for-1.8.6.zip"
        else
          echo "⚠️ Warning: Cache file not found, pyhanlp may download automatically on first use"
        fi
    
    - name: Test API script syntax
      run: |
        echo "=== 代码语法检查 ==="
        echo "检查 rel_api_optimized.py..."
        python -m py_compile rel_api_optimized.py && echo "✅ rel_api_optimized.py 语法正确" || {
          echo "❌ rel_api_optimized.py 语法错误"
          exit 1
        }
        echo "✅ 代码语法检查通过"
    
    - name: Verify Python imports
      run: |
        echo "=== 验证 Python 导入 ==="
        echo "测试基础依赖..."
        python -c "import tqdm; print('✅ tqdm')" || echo "❌ tqdm 导入失败"
        python -c "import numpy; print('✅ numpy', numpy.__version__)" || echo "❌ numpy 导入失败"
        python -c "import networkx; print('✅ networkx', networkx.__version__)" || echo "❌ networkx 导入失败"
        python -c "import matplotlib; print('✅ matplotlib', matplotlib.__version__)" || echo "❌ matplotlib 导入失败"
        python -c "import pandas; print('✅ pandas')" || echo "❌ pandas 导入失败"
        python -c "import requests; print('✅ requests')" || echo "❌ requests 导入失败"
        echo "测试 HanLP..."
        python -c "import pyhanlp; print('✅ PyHanLP import successful')" || echo "⚠️ PyHanLP import failed (may need data file)"
      continue-on-error: true
    
    - name: Test API extraction (if API key provided)
      env:
        CI: true
        MPLBACKEND: Agg
        # 如果使用 Environment secrets，这里的 secrets 会自动从环境获取
        # 如果使用 Repository secrets，直接使用 secrets.XXX
        API_BASE_URL: ${{ secrets.API_BASE_URL || 'https://miaodi.zeabur.app' }}
        API_KEY: ${{ secrets.API_KEY }}
        API_MODEL: ${{ secrets.API_MODEL || 'deepseek-ai/DeepSeek-V3-0324' }}
      run: |
        echo "=== API 配置检查 ==="
        if [ -n "$API_KEY" ]; then
          echo "✅ API_KEY 已设置（长度: ${#API_KEY} 字符）"
        else
          echo "⚠️ API_KEY 未设置"
        fi
        echo "API_BASE_URL: $API_BASE_URL"
        echo "API_MODEL: $API_MODEL"
        echo ""
        
        mkdir -p output
        echo "=== 检查测试书籍 ==="
        if [ -d "book" ]; then
          echo "✅ book/ 目录存在"
          echo "book/ 目录内容:"
          ls -lah book/ 2>/dev/null || echo "无法列出 book/ 目录内容"
        else
          echo "⚠️ book/ 目录不存在"
        fi
        
        # 检查 API 密钥是否设置
        if [ -z "$API_KEY" ]; then
          echo ""
          echo "⚠️ API_KEY 未设置，跳过实际运行测试"
          echo "💡 提示: 在 GitHub Secrets 中设置 API_KEY 以启用完整测试"
          echo "   - Repository Secrets: Settings → Secrets and variables → Actions → Secrets"
          echo "   - Environment Secrets: Settings → Environments → [环境名] → Secrets"
          echo "✅ 仅进行语法检查"
          exit 0
        fi
        
        # 查找测试书籍
        echo ""
        echo "=== 查找测试书籍 ==="
        BOOK_FILE=$(find book -maxdepth 1 -name "*.txt" -type f | head -n 1)
        echo "找到的书籍文件: ${BOOK_FILE:-无}"
        
        if [ -n "$BOOK_FILE" ] && [ -f "$BOOK_FILE" ]; then
          BOOK_NAME=$(basename "$BOOK_FILE" .txt)
          BOOK_SIZE=$(du -h "$BOOK_FILE" | cut -f1)
          echo ""
          echo "=== 测试书籍信息 ==="
          echo "✅ 找到测试书籍: $BOOK_NAME"
          echo "📄 文件路径: $BOOK_FILE"
          echo "📊 文件大小: $BOOK_SIZE"
          echo ""
          echo "=== API 配置 ==="
          echo "📡 API 地址: $API_BASE_URL"
          echo "📦 模型: $API_MODEL"
          echo ""
          echo "=== 运行参数 ==="
          echo "max_sentences: 50"
          echo "context_lines: 2"
          echo ""
          echo "🚀 开始 API 优化版测试..."
          
          # 使用较小的参数，避免超时和过多 token 消耗
          python rel_api_optimized.py \
            --book "$BOOK_NAME" \
            --base_url "$API_BASE_URL" \
            --api_key "$API_KEY" \
            --model "$API_MODEL" \
            --max_sentences 50 \
            --context_lines 2 \
            --output output || {
            EXIT_CODE=$?
            echo ""
            echo "=== API 测试失败 ==="
            echo "退出码: $EXIT_CODE"
            echo "⚠️ API 测试失败，可能的原因："
            echo "   - API 密钥无效或过期"
            echo "   - API 服务不可用或超时"
            echo "   - 网络连接问题"
            echo "   - 模型名称错误"
            echo "   - 请求格式错误"
            echo ""
            echo "💡 排查建议："
            echo "   1. 检查 GitHub Secrets 中的 API_KEY 是否正确"
            echo "   2. 检查 API_BASE_URL 是否可访问"
            echo "   3. 检查 API_MODEL 是否正确"
            echo "   4. 查看上方详细错误信息"
            exit 0  # 允许失败，不阻塞 CI
          }
          
          echo ""
          echo "=== 测试完成 ==="
          if [ -d "output" ]; then
            echo "✅ output/ 目录存在"
            echo "生成的文件:"
            ls -lah output/ || echo "无法列出 output/ 目录内容"
          else
            echo "⚠️ output/ 目录不存在"
          fi
        else
          echo "⚠️ 未找到测试书籍，跳过 API 实际运行测试"
          echo "💡 仅进行语法检查"
          echo ""
          echo "提示: 确保 book/ 目录下有 .txt 格式的测试书籍文件"
        fi
      continue-on-error: true
    
    - name: Log output files before upload
      if: always()
      run: |
        echo "=== 准备上传 Artifact ==="
        if [ -d "output" ]; then
          echo "✅ output/ 目录存在"
          echo "文件列表:"
          find output -type f -exec ls -lh {} \; || echo "无法列出文件"
          echo ""
          echo "文件统计:"
          find output -type f | wc -l | xargs echo "文件数量:"
        else
          echo "⚠️ output/ 目录不存在，将创建空 artifact"
        fi
      continue-on-error: true
    
    - name: Upload API results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: api-optimized-results
        path: output/
        if-no-files-found: warn
        retention-days: 7
      continue-on-error: true
    
    - name: Log upload status
      if: always()
      run: |
        echo "=== Artifact 上传状态 ==="
        echo "Artifact 名称: api-optimized-results"
        echo "保留天数: 7"
        echo "✅ 上传完成（如果未找到文件会显示警告）"
      continue-on-error: true

  lint:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.8"
    
    - name: Install linting tools
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pylint
    
    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
      continue-on-error: true
